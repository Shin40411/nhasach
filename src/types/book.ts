export type IBookFilters = {
    subject: string;
    grade: string;
    author: string;
};

export type IBookItem = {
    id: string;
    title: string;
    subject: string;
    grade: string;
    author: string;
};
